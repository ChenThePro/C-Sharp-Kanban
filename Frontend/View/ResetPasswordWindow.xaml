<Window x:Class="Frontend.View.ResetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Reset Password"
        Height="300" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <materialDesign:Card Margin="20" Padding="20" materialDesign:ElevationAssist.Elevation="Dp2">
            <StackPanel>
                <TextBlock Text="Enter your email to reset password"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0 0 0 20"/>

                <TextBox x:Name="EmailBox"
                         Margin="0 10"
                         materialDesign:HintAssist.Hint="Email"
                         Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Button Content="Send Code"
                        Click="SendCode_Click"
                        HorizontalAlignment="Center"
                        Margin="0 20 0 0"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>