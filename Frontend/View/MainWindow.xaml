<Window x:Class="Frontend.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Frontend.View"
        xmlns:vm="clr-namespace:Frontend.ViewModel"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Welcome to Kanban"
        Height="800" Width="1000"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <ToggleButton x:Name="ThemeToggle"
                      Width="50" Height="50"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Top"
                      Margin="0,15,15,0"
                      IsChecked="{Binding IsDarkTheme, Mode=TwoWay}"
                      Checked="ThemeToggle_Checked"
                      Unchecked="ThemeToggle_Unchecked"
                      ToolTip="Toggle theme"
                      Style="{StaticResource MaterialDesignActionToggleButton}"
                      Background="Transparent"
                      BorderBrush="Transparent">
            <ToggleButton.Content>
                <Grid>
                    <!-- Sun Icon for Light Mode (visible when NOT in dark mode) -->
                    <TextBlock Text="☀"
                               FontSize="24"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource PrimaryBrush}"
                               Visibility="{Binding IsDarkTheme, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

                    <!-- Moon Icon for Dark Mode (visible when in dark mode) -->
                    <TextBlock Text="☾"
                               FontSize="24"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource PrimaryBrush}"
                               Visibility="{Binding IsDarkTheme, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </ToggleButton.Content>
        </ToggleButton>

        <materialDesign:Card
                             Background="{DynamicResource BackgroundBrush}"
                             Margin="40"
                             Padding="40"
                             BorderBrush="{DynamicResource ForegroundBrush}"
                             BorderThickness="1"
                             materialDesign:ElevationAssist.Elevation="Dp3">

            <TabControl Background="Transparent" FontSize="14" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem" BasedOn="{StaticResource MaterialDesignTabItem}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Grid>
                                        <Border Background="Transparent"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                                            <ContentPresenter x:Name="ContentSite"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          ContentSource="Header"
                                          RecognizesAccessKey="True"
                                          Margin="{TemplateBinding Padding}" />
                                        </Border>
                                        <Border x:Name="SelectedIndicator"
                            Height="2"
                            VerticalAlignment="Bottom"
                            Background="Transparent" />
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="{DynamicResource PrimaryBrush}"/>
                                            <Setter TargetName="SelectedIndicator" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="{DynamicResource SecondaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Padding" Value="15,8"/>
                        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </TabControl.Resources>

                <TabItem Header="Sign In">
                    <StackPanel Margin="30" VerticalAlignment="Center">
                        <TextBlock Text="Welcome Back"
                                   FontSize="26"
                                   FontWeight="Bold"
                                   Margin="0 0 0 30"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource ForegroundBrush}"/>

                        <TextBox Margin="0 10"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                 materialDesign:HintAssist.Hint="Email"
                                 materialDesign:HintAssist.IsFloating="True"
                                 Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <Grid Margin="0 10" x:Name="SignInPasswordGrid" />

                        <Button x:Name="ForgotPasswordButton"
                                Content="Forgot Password?"
                                Click="ForgotPassword_Click"
                                Visibility="Collapsed"
                                Style="{StaticResource MaterialDesignFlatButton}"
                                HorizontalAlignment="Center"
                                Margin="0 10 0 0"
                                Foreground="{DynamicResource SecondaryBrush}"/>

                        <Button Content="Sign In"
                                Click="SignIn_Click"
                                Margin="0 30"
                                HorizontalAlignment="Center"
                                Width="150" Height="45"
                                Background="{DynamicResource PrimaryBrush}"
                                Foreground="{DynamicResource ForegroundBrush}"
                                FontWeight="Bold"
                                FontSize="16"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                materialDesign:ButtonAssist.CornerRadius="20"/>
                    </StackPanel>
                </TabItem>

                <TabItem Header="Sign Up">
                    <StackPanel Margin="30" VerticalAlignment="Center">
                        <TextBlock Text="Create an Account"
                                   FontSize="26"
                                   FontWeight="Bold"
                                   Margin="0 0 0 30"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource ForegroundBrush}"/>

                        <TextBox Margin="0 10"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                 materialDesign:HintAssist.Hint="Email"
                                 materialDesign:HintAssist.IsFloating="True"
                                 Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <Grid Margin="0 10" x:Name="SignUpPasswordGrid" />
                        <Grid Margin="0 10" x:Name="ConfirmPasswordGrid" />

                        <Button Content="Sign Up"
                                Click="SignUp_Click"
                                Margin="0 30"
                                HorizontalAlignment="Center"
                                Width="150" Height="45"
                                Background="{DynamicResource PrimaryBrush}"
                                Foreground="{DynamicResource ForegroundBrush}"
                                FontWeight="Bold"
                                FontSize="16"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                materialDesign:ButtonAssist.CornerRadius="20"/>
                    </StackPanel>
                </TabItem>
            </TabControl>
        </materialDesign:Card>
    </Grid>
</Window>